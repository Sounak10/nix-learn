# TEMPLATE ONLY. This file has no usable recipient.
# Copy to .sops.yaml and replace every placeholder with a real PUBLIC age
# recipient before creating an encrypted file.
creation_rules:
  - path_regex: 'secrets\.yaml$'
    key_groups:
      - age:
          - 'age1HOST_PUBLIC_RECIPIENT_REPLACE_ME'
          - 'age1RECOVERY_PUBLIC_RECIPIENT_REPLACE_ME'
